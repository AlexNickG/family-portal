# Family Portal

Веб-приложение для публичного просмотра базы контактов, хранящейся в Airtable.

## 🚀 Возможности

- Просмотр контактов с фотографиями в виде карточек
- Поиск по имени, компании, заметкам и тегам
- Фильтрация по категориям
- Детальный просмотр контакта в модальном окне
- Галерея фотографий с lightbox
- Пагинация для больших баз данных
- Адаптивный дизайн для мобильных устройств
- Ссылки на социальные сети (LinkedIn, Facebook, Instagram, Twitter, Telegram)

## 📋 Требования

- Node.js 18+ 
- Аккаунт Airtable
- Аккаунт Vercel (для деплоя)

## 🛠 Установка

### 1. Подготовка Airtable

Создайте таблицу с именем **Relatives** и следующими полями:

| Поле | Тип | Описание |
|------|-----|----------|
| Name | Single line text | Имя |
| Surname | Single line text | Фамилия |
| Position | Single line text | Должность |
| Company | Single line text | Компания |
| Phone | Phone number | Телефон |
| Email | Email | Email |
| LinkedIn | URL | Ссылка на LinkedIn |
| Facebook | URL | Ссылка на Facebook |
| Instagram | URL | Ссылка на Instagram |
| Twitter | URL | Ссылка на Twitter/X |
| Telegram | URL | Ссылка на Telegram |
| Category | Single line text | Категории через запятую |
| Notes | Long text | Заметки |
| Photos | Attachment | Фотографии (можно несколько) |

### 2. Получите учетные данные Airtable

1. **API Key**: 
   - Перейдите на https://airtable.com/account
   - Нажмите "Generate API key"
   - Скопируйте ключ

2. **Base ID**:
   - Откройте вашу базу в Airtable
   - В URL будет `airtable.com/appXXXXXXXXXXXXXX`
   - `appXXXXXXXXXXXXXX` — это ваш Base ID

### 3. Клонируйте и настройте проект

```bash
# Создайте новый проект Next.js
npx create-next-app@latest family-portal
cd family-portal

# Установите зависимости
npm install airtable lucide-react
```

При создании проекта выберите:
- TypeScript: **No**
- ESLint: **Yes**
- Tailwind CSS: **Yes**
- `src/` directory: **No**
- App Router: **No** (используем Pages Router)
- Import alias: **Yes** (@/*)

### 4. Скопируйте все файлы из артефактов

Создайте структуру папок и скопируйте код из всех артефактов выше:

```
family-portal/
├── lib/
│   └── airtable.js
├── pages/
│   ├── api/
│   │   ├── contacts.js
│   │   └── contacts/
│   │       └── [id].js
│   └── index.js
├── components/
│   ├── ContactCard.js
│   ├── ContactModal.js
│   ├── SearchBar.js
│   ├── FilterBar.js
│   ├── Pagination.js
│   └── PhotoGallery.js
├── .env.local
├── .env.example
├── next.config.js
└── package.json
```

### 5. Настройте переменные окружения

Создайте файл `.env.local`:

```bash
AIRTABLE_API_KEY=ваш_api_key
AIRTABLE_BASE_ID=ваш_base_id
AIRTABLE_TABLE_NAME=Relatives
```

**Важно:** Добавьте `.env.local` в `.gitignore`!

### 6. Запустите локально

```bash
npm run dev
```

Откройте http://localhost:3000

## 🚀 Деплой на Vercel

### Вариант 1: Через GitHub (рекомендуется)

1. Создайте репозиторий на GitHub
2. Закоммитьте код (без `.env.local`!)
3. Зайдите на https://vercel.com
4. Нажмите "New Project"
5. Импортируйте репозиторий из GitHub
6. Добавьте переменные окружения:
   - `AIRTABLE_API_KEY`
   - `AIRTABLE_BASE_ID`
   - `AIRTABLE_TABLE_NAME`
7. Нажмите "Deploy"

### Вариант 2: Через Vercel CLI

```bash
npm i -g vercel
vercel login
vercel
```

Следуйте инструкциям в терминале.

## 🔒 Безопасность

- API ключ Airtable хранится в переменных окружения и недоступен клиенту
- Все запросы к Airtable проходят через Vercel Serverless Functions
- `.env.local` добавлен в `.gitignore` и не попадает в репозиторий

## 🎨 Кастомизация

### Изменение количества карточек на странице

В `pages/index.js` измените значение `pageSize`:

```javascript
const params = new URLSearchParams({
  page: currentPage.toString(),
  pageSize: '24', // Измените на нужное значение
  // ...
});
```

### Изменение цветовой схемы

Отредактируйте Tailwind классы в компонентах. Например, для основного цвета:
- `bg-gray-900` → `bg-blue-900`
- `text-gray-900` → `text-blue-900`

### Добавление логотипа

Замените заголовок в `pages/index.js`:

```javascript
<h1 className="text-3xl font-semibold text-gray-900">
  Family Portal
</h1>
```

На изображение:

```javascript
<img src="/logo.png" alt="Family Portal" className="h-12" />
```

## 📱 Мобильная версия

Приложение полностью адаптивно:
- На мобильных: 1 колонка карточек
- На планшетах: 2 колонки
- На десктопах: 3-4 колонки

## 🐛 Решение проблем

### Ошибка "Failed to fetch contacts"

- Проверьте правильность `AIRTABLE_API_KEY` и `AIRTABLE_BASE_ID`
- Убедитесь, что таблица называется `Relatives`
- Проверьте, что переменные окружения добавлены в Vercel

### Фотографии не загружаются

- Убедитесь, что домены Airtable добавлены в `next.config.js`
- Проверьте, что поле `Photos` в Airtable имеет тип "Attachment"

### Поиск не работает

- Проверьте названия полей в Airtable (должны совпадать с кодом)
- Убедитесь, что используете корректный регистр (Name, Surname, и т.д.)

## 📊 Производительность

- Пагинация: 24 контакта на страницу (настраивается)
- Кэширование: Vercel Edge Network
- Оптимизация изображений: автоматическая через Airtable thumbnails
- API Rate Limit Airtable: 5 запросов в секунду

## 📄 Лицензия

MIT

## 🤝 Поддержка

При возникновении проблем:
1. Проверьте документацию Airtable API: https://airtable.com/api
2. Проверьте документацию Vercel: https://vercel.com/docs
3. Проверьте логи в Vercel Dashboard

## 🎯 Будущие улучшения

- [ ] Экспорт контактов в CSV/vCard
- [ ] Расширенные фильтры
- [ ] Избранное
- [ ] Темная тема
- [ ] Многоязычность